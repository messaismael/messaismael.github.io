/**
 * Public site configuration.
 *
 * Keep non-secret, build-time values here. Never place credentials or private
 * API keys in this file: it is bundled into the client application.
 */
export const siteConfig = {
	analytics: {
		gtag: 'G-PR9V1BTVRC'
	}
} as const;
